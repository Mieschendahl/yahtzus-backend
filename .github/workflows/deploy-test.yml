  name: Deploy test backend

  on:
    push:
      branches:
        - test

  jobs:
    deploy:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy backend to test server
          uses: appleboy/ssh-action@v1.0.3
          with:
            host: 91.99.109.255
            username: deploy
            key: ${{ secrets.SERVER_SSH_KEY }}
            script: |
              cd /opt/yahtzus/test/backend
              git pull origin test
              docker compose up -d --build yahtzus-test-backend
              docker compose ps